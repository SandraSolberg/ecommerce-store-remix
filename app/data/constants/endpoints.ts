const baseURL = 'http://localhost:5173/';

const endpoint = {
  FOODGROUPS: `${baseURL}app/data/foodGroups.json`,
  FOODS: `${baseURL}app/data/foods.json`,
  RECIPES: `${baseURL}app/data/recipes.json`,
};

export default endpoint;
